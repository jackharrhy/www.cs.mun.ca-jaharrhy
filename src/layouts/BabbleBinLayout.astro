---
import Feed from "@components/Feed.astro";
import Layout, { Props as LayoutProps } from "@layouts/Layout.astro";

export interface Props extends LayoutProps {}

const { title } = Astro.props as Props;
---

<style is:global>
  :root {
    font-size: 22px;
    color-scheme: only light;
  }

  body {
    padding: 0 1rem 2rem 1rem;

    font-family:
      Iowan Old Style,
      Apple Garamond,
      Baskerville,
      Times New Roman,
      Droid Serif,
      Times,
      Source Serif Pro,
      serif,
      Apple Color Emoji,
      Segoe UI Emoji,
      Segoe UI Symbol;
  }

  header {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  header a {
    color: black;
    font-size: 2rem;
    font-weight: bold;
    margin: 1rem 0;
    text-decoration-thickness: 1px;
  }

  header img {
    filter: grayscale(0.9);
    transition: filter 0.2s;
  }

  header img:hover {
    filter: grayscale(0.4);
  }

  header,
  main {
    margin: 0 auto;
    max-width: 60ch;
  }

  ul,
  ol {
    padding-left: 0;
  }

  blockquote {
    margin: 0;
    padding: 0;
    padding-left: 0.75rem;
    font-style: italic;
    border-left: 0.25rem solid #ccc;
  }

  .astro-code {
    padding: 0.5rem 0.6rem;
  }
  code {
    font-size: 0.75rem;
  }
</style>

<Layout title={title}>
  <meta
    name="viewport"
    content="width=device-width, initial-scale=1"
    slot="head"
  />
  <header>
    <a href={`/~jaharrhy/babble-bin/`}>babble bin</a>
    <a href={`/~jaharrhy/babble-bin/rss.xml`}>
      <Feed />
    </a>
  </header>
  <main>
    <slot />
  </main>
</Layout>
