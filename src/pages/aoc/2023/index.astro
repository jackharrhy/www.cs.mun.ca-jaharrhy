---
import { Image } from "astro:assets";
import theBoyImage from "@images/aoc/2023/the-boy.jpg";
import AdventOfCodeLayout from "@layouts/AdventOfCodeLayout.astro";

const days = Object.keys(import.meta.glob("./day*.mdx")).map((fileName) =>
  Number(fileName.split(".")[1].slice(4))
);
---

<style>
  h2 {
    font-size: 2rem;
    text-align: center;
  }
  ul {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    font-size: 1.5rem;
    text-align: center;
    list-style: none;
  }

  .tips {
    margin-top: 2rem;
  }

  .jack-utils {
    margin-top: 2rem;
    opacity: 0;
    pointer-events: none;
  }

  .jack-utils:focus {
    opacity: 1;
  }

  .boy {
    display: flex;
    justify-content: center;
  }
</style>

<AdventOfCodeLayout year={2023} title="advent of code 2023">
  <p>
    i did not very good this advent of code, and while i did have a day 1 ramble
    in progress, i never finished it off
  </p>
  <p>here is a picture of peanut to make up for it:</p>
  <div class="boy">
    <Image
      src={theBoyImage}
      alt="orange cat sitting on pink blanket"
      width={500}
    />
  </div>
  <ul>
    {
      days.map((day) => (
        <a href={`./day${day}/`}>
          <li>[day {day}]</li>
        </a>
      ))
    }
    <a href={`./jack-utils/`} class="jack-utils">
      <li>[jack-utils]</li>
    </a>
  </ul>
</AdventOfCodeLayout>
