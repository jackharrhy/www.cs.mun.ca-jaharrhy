---
const { height } = Astro.props;
---

<div>
    <slot />
</div>

<style define:vars={{ height }}>
    div {
        margin: 1.5rem 0;
        height: var(--height, auto);
        display: flex;
        justify-content: center;
        gap: 1rem;
    }
</style>
